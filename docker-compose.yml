services:
  f1-web-helper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: f1-web-helper
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
